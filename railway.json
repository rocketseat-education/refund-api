{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "pakagename/railpack",
    "buildCommand": "npm run build"
  },
  "start": {
    "startCommand": "cd build && npm ci --omit=dev && node ace migration:run --force && node bin/server.js"
  }
}
